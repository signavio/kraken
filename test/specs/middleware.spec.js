describe.skip('middleware', function () {

  describe('LOAD_ENTITY action', function () {

    it('should only dispatch a REQUEST action if the entity is not already cached')
    it('should only dispatch a REQUEST action if a request is not already pending')
    it('should dispatch a REQUEST action a cached entity misses some required fields')
  })
})
